const addFundsSchema = require('./add-funds');
const transferSchema = require('./transfer');
const withdrawSchema = require('./withdraw');
const historySchema = require('./history');

module.exports = { addFundsSchema, transferSchema, withdrawSchema, historySchema };
